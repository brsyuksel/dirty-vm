name: "create"
help: "Usage for create"
readme: "README.md"
run: "echo 'Hello from create'"
# before: "echo 'Running before'"
# after: "echo 'Running after'"
environments:
  RUNNABLE_ENV: "value"

# TODO
# bridge, ip addr, iptables rules
# dnsmasq, static ip config
# cloud init: user data, metadata, cloud-init iso
# mac addr - ip addr
# qemu params
# disk creation
