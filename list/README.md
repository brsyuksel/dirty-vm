# list

Lists all created virtual machines and their resources.

## Usage

```bash
dirty-vm list
```

## Output Columns

- **name** — VM name
- **ip** — assigned IP address
- **vcpu** — number of virtual CPU cores
- **memory** — memory size in GB
- **disc** — disc size in GB

If no VMs exist, the command exits silently.
