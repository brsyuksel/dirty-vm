name: "dirty-vm"
help: "Manages virtual machines created for dirty jobs"
readme: "README.md"
environments:
  DIRTY_VM_HOME: "~/.dirty-vm"
runnables:
  - setup
  - check
  - images
  - pull
  - delete-image
  - create
  - list
  - start
  - stop
  - delete
  - run
  - ssh
  - forward
